name: Manga Monitor

on:
  schedule:
    # Мониторинг каждые 10 минут, но только 10 скачиваний за раз
    - cron: '*/10 * * * *'
  workflow_dispatch: # Позволяет запускать вручную
  push:
    branches: [ '**' ]
    # Без paths: запускать на любой измененный файл в любой ветке

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.PAT_TOKEN || secrets.GITHUB_TOKEN }}
    
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: rustfmt, clippy
    
    - name: Cache cargo dependencies (limited)
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
    
    - name: Clean up disk space (aggressive)
      run: |
        # Максимально агрессивная очистка диска
        sudo rm -rf /tmp/*
        sudo rm -rf ~/.cargo/registry/cache/*
        sudo rm -rf ~/.cargo/registry/src/*
        sudo rm -rf ~/.cargo/git/db/*
        sudo rm -rf /var/cache/*
        sudo rm -rf /var/log/*
        sudo rm -rf /home/runner/.cache/*
        sudo rm -rf /usr/share/doc/*
        sudo rm -rf /usr/share/man/*
        sudo rm -rf /usr/share/locale/*
        sudo rm -rf /usr/share/info/*
        sudo apt-get clean
        sudo apt-get autoclean
        sudo apt-get autoremove -y
        # Очищаем ВСЕ скачанные файлы чтобы освободить максимум места
        rm -rf downloads/* 2>/dev/null || true
        rm -rf scans/* 2>/dev/null || true
        # Очищаем кэш cargo полностью
        rm -rf ~/.cargo/registry/cache/* 2>/dev/null || true
        rm -rf ~/.cargo/git/db/* 2>/dev/null || true
        df -h
        
    - name: Build guloader
      run: cargo build --release
    
    - name: Configure Git
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "github-actions[bot]"
        git config --global --add safe.directory "${{ github.workspace }}"
        git config --local pull.rebase true
        git config --local rebase.autoStash true
    
    - name: Run manga monitoring (10-min, limited load)
      run: |
        # Создаем директории если их нет
        mkdir -p data downloads scans
        
        # Инициализируем базу данных если её нет
        if [ ! -f "data/guloader.db" ]; then
          echo "Initializing database..."
          ./target/release/guloader --config config.toml init
        fi
        
        # Запускаем мониторинг с ограниченной нагрузкой
        echo "Starting manga monitoring (every 10 min, limit 10 chapter downloads)..."
        echo "Will scan ALL manga, but download at most 10 chapters..."
        
        # Сканируем всех, но ограничиваем СКАЧИВАНИЯ глав числом 10
        ./target/release/guloader --config config.toml monitor --auto-commit --limit 10
        
        # Показываем статус после мониторинга
        echo "Monitoring completed. Showing status..."
        ./target/release/guloader --config config.toml status
        
        # Максимальная очистка после выполнения
        echo "Cleaning up all temporary files..."
        find data/ -name "*.db-journal" -delete 2>/dev/null || true
        # Удаляем ВСЕ скачанные файлы чтобы освободить максимум места
        rm -rf downloads/* 2>/dev/null || true
        rm -rf scans/* 2>/dev/null || true
        
        # Дополнительная очистка системных файлов
        sudo rm -rf /tmp/*
        sudo rm -rf ~/.cargo/registry/cache/*
        sudo rm -rf ~/.cargo/git/db/*
        sudo rm -rf /home/runner/.cache/*
        df -h
    
    - name: Commit changes
      run: |
        # Проверяем есть ли изменения
        if [ -n "$(git status --porcelain)" ]; then
          echo "Changes detected, committing..."
          git add .
          
          # Создаем информативное сообщение коммита
          COMMIT_MSG="🤖 Auto-update: $(date '+%Y-%m-%d %H:%M:%S UTC')\n\n$(git status --porcelain | head -20)\nRun ID: ${{ github.run_id }}"
          
          git commit -m "$COMMIT_MSG"
          # Обновляем локальную ветку перед пушем, чтобы избежать расхождений
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"
          echo "Changes committed successfully"
        else
          echo "No changes to commit"
        fi
    
    - name: Upload logs
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: error-logs
        path: |
          data/
          *.log
